# GPXFixer

Kotlin Multiplatform приложение для импорта, просмотра, базового редактирования и
экспорта GPX-файлов.

Проект уже умеет выбирать GPX-файл на Android и iOS, парсить его в общей KMP-логике,
сохранять исходный файл, вести локальную историю импортированных треков, показывать
трек на карте, обрезать трек по началу/концу, удалять конкретные точки и
экспортировать результат.

## Текущий стек

- Kotlin Multiplatform
- Compose Multiplatform
- Android + iOS targets
- Gradle Kotlin DSL

## Модули

- `shared` — общий KMP-модуль с доменными моделями GPX, парсером/сериализатором,
  use case импорта, просмотра, обрезки и удаления точек, iOS facade и тестами.
- `app-android` — Android-приложение с выбором GPX-файла, импортом, локальным
  хранением исходников, списком импортированных треков, просмотром, trim flow и
  edit flow и экспортом.
- `iosApp` — iOS-приложение на SwiftUI с выбором GPX-файла, импортом, локальной
  историей импортированных треков, просмотром, trim/edit flow и шарингом GPX.

## Что реализовано на этом этапе

- Инициализирована сборка Gradle (wrapper + Kotlin DSL).
- Настроены KMP targets: Android и iOS (`iosX64`, `iosArm64`, `iosSimulatorArm64`).
- Настроен Compose Multiplatform.
- Добавлены доменные модели GPX-документа, metadata, треков, сегментов и точек.
- Реализован GPX parser с поддержкой namespace, escaped text, CDATA и self-closing tags.
- Реализован GPX serializer для обратной записи документа в XML.
- Реализован общий `ImportGpxTrackUseCase`:
  - валидирует GPX через parser;
  - выбирает display name из metadata, имени трека или имени файла;
  - сохраняет исходный GPX-файл;
  - записывает metadata импорта;
  - откатывает сохраненный файл, если запись metadata не удалась.
- Реализован общий `TrackDetailUseCase`:
  - загружает сохраненный GPX по `storageKey`;
  - повторно парсит файл в `GpxDocument`;
  - считает количество треков, сегментов и точек;
  - считает дистанцию, набор/сброс высоты и диапазон высот;
  - определяет старт/финиш и временной диапазон;
  - возвращает предупреждения по неполным данным.
- Реализован общий `TrimGpxTrackUseCase`:
  - обрезает `GpxDocument` по глобальному диапазону точек;
  - сохраняет структуру треков и сегментов, удаляя пустые элементы;
  - пересчитывает summary для измененного документа;
  - возвращает количество удаленных точек;
  - валидирует пустые документы и некорректные диапазоны.
- Реализован общий `DeleteGpxTrackPointUseCase`:
  - удаляет точку по глобальному индексу;
  - сохраняет структуру треков и сегментов, удаляя пустые элементы;
  - пересчитывает summary для измененного документа;
  - валидирует пустые документы и некорректные индексы.
- Android:
  - выбор GPX через системный document picker;
  - импорт GPX из `ACTION_VIEW`;
  - локальное хранение исходных GPX-файлов;
  - JSON-хранилище истории импортов;
  - экран со статусом импорта и списком импортированных треков;
  - экран просмотра импортированного трека по нажатию на элемент списка;
  - read-only просмотр линии трека на Google Maps;
  - полноэкранная карта трека по нажатию на preview;
  - экран обрезки трека с предпросмотром summary;
  - сохранение обрезанного GPX поверх импортированного трека;
  - экран редактирования, где карта занимает весь экран;
  - кликабельные точки трека с выделением выбранной точки;
  - меню выбранной точки с индексом, удалением и заглушкой перемещения;
  - сохранение изменений поверх импортированного трека;
  - экспорт GPX через системный share sheet.
- iOS:
  - выбор GPX через `UIDocumentPickerViewController`;
  - импорт GPX из `.onOpenURL`;
  - импорт через общий KMP `ImportGpxTrackUseCase`;
  - просмотр импортированного трека через общий KMP `TrackDetailUseCase`;
  - read-only просмотр линии трека на Apple MapKit;
  - iOS-реализации storage/store/id/clock в `shared/src/iosMain`;
  - локальное хранение исходных GPX-файлов;
  - JSON-хранилище истории импортов;
  - SwiftUI-экран со статусом импорта, списком импортированных треков и экраном
    деталей трека;
  - read-only preview карты и полноэкранная карта трека;
  - экран обрезки трека с предпросмотром summary;
  - сохранение обрезанного GPX поверх импортированного трека;
  - экран редактирования, где карта занимает весь экран;
  - кликабельные точки трека с выделением выбранной точки;
  - меню выбранной точки с индексом, удалением и заглушкой перемещения;
  - сохранение изменений поверх импортированного трека;
  - экспорт GPX через системный share sheet.
- Добавлены common tests для GPX parser/serializer, use case импорта, summary
  импортированного трека, обрезки GPX и удаления точек.

## Проверка

```bash
./gradlew test
```

## Google Maps API key для Android

Android-карта использует Maps SDK for Android через Google Maps Compose. Ключ не
коммитится: добавьте его локально в `local.properties` в корне проекта:

```properties
MAPS_API_KEY=your_api_key_here
```

Получить ключ можно в Google Cloud Console:

1. Создать или выбрать Google Cloud project.
2. Включить `Maps SDK for Android`.
3. Создать API key в разделе `APIs & Services` -> `Credentials`.
4. Ограничить ключ для Android-приложения `com.gpxeditor.android` и подписывающего
   сертификата SHA-1.

## Что намеренно НЕ реализовано

- Редактируемая карта
- Перемещение точек
- Split/merge сегментов
- Undo/history изменений
- Сохранение отредактированного трека как отдельной копии
- Сглаживание высоты
- Переименование трека
- Backend
- DI
- Навигация
- Локальная БД вместо JSON-файлов
- Сетевой слой

## Ближайший логичный следующий шаг

Закрыть текущий вертикальный срез удаления точек и перейти к более удобному
редактированию:

- вручную проверить сценарий import -> detail -> edit -> delete point -> save -> reopen
  на Android и iOS;
- прогнать кейсы с multi-track, multi-segment, одной точкой, отсутствующей высотой,
  отсутствующим временем и большим количеством точек;
- добавить undo/reset для текущей edit-сессии;
- реализовать перемещение выбранной точки;
- добавить удаление диапазона точек как ускорение для чистки выбросов.

После этого можно расширять редактирование: split/merge сегментов, сглаживание
высоты и переименование трека.
